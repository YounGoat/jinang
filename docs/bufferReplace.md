##	jinang/bufferReplace

Return a new buffer with all matches replaced.

*   Buffer __bufferReplace__( Buffer *source*, Buffer *search*, Buffer *replace* )  
    Return a new buffer with all matches replaced.